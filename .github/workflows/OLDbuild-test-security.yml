#name: Build, Test, and Security Scan
#
#on:
#  push:
#    branches: [ main ]
#  pull_request:
#    branches: [ main ]
#
#jobs:
#  build:
#    runs-on: ubuntu-latest
#    steps:   
#      - uses: actions/checkout@v3
#
#      - name: Setup .NET
#        uses: actions/setup-dotnet@v3
#        with:
#          dotnet-version: 7.0.x
#
#      - name: Restore dependencies
#        run: dotnet restore ExpenseSystem.sln
#
 #     - name: Build
 #       run: dotnet build ExpenseSystem.sln --no-restore

      #- name: Test
      #  run: dotnet test --no-build --verbosity normal

      
      # 1. Code Review (Touchpoint #1)
      # 2. Code Review (Touchpoint #1)
      # 3. Code Review (Touchpoint #1)
      # 4. Code Review (Touchpoint #1)
      # 5. Code Review (Touchpoint #1)
      # 6. Code Review (Touchpoint #1)
      # 7. Code Review (Touchpoint #1)